#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    double ft;
    cin >> ft;

    cout << fixed << setprecision(1);
    cout << ft * 30.48;
}